export class Weight{
  imperial: string;
  metric: string;

  constructor(imperial: string, metric: string) {
    this.imperial = imperial;
    this.metric = metric;
  }
  /*
  public toString(): string {
    //can do by location
  }
   */
}
